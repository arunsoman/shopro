package mls.sho.dms.common.enums;

/**
 * Distinguishes between final menu items and intermediate batch preparations.
 */
public enum RecipeType {
    PLATE,    // Final sellable item (e.g. "Kabuli Pulao")
    BATCH     // Intermediate prepared item (e.g. "Korma Base")
}
