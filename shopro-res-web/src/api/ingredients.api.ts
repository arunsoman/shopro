/*
 * list(), search(), getById(), getCosts(), create(),
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_ingredients_api = true;

// You can add logic for ingredients.api here based on:
// list(), search(), getById(), getCosts(), create(),
