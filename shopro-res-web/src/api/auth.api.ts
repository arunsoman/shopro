/*
 * login(), logout(), refreshToken(), resetPassword()
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_auth_api = true;

// You can add logic for auth.api here based on:
// login(), logout(), refreshToken(), resetPassword()
