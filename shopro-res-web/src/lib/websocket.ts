/*
 * createFloorSocket(restaurantId, onMessage) — POS live feed
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_websocket = true;

// You can add logic for websocket here based on:
// createFloorSocket(restaurantId, onMessage) — POS live feed
