/*
 * suggestPlu(name): string — auto-generate PLU from name
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_plu = true;

// You can add logic for plu here based on:
// suggestPlu(name): string — auto-generate PLU from name
