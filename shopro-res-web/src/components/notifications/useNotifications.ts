/*
 * aggregates alerts from 4 endpoints + unread count
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_useNotifications = true;

// You can add logic for useNotifications here based on:
// aggregates alerts from 4 endpoints + unread count
