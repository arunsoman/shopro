/*
 * useAutosave(saveFn, deps, delayMs) — wizard autosave
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_useAutosave = true;

// You can add logic for useAutosave here based on:
// useAutosave(saveFn, deps, delayMs) — wizard autosave
