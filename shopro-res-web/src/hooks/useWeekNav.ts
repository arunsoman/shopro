/*
 * useWeekNav(initial) → {weekStart, next, prev, setWeek}
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_useWeekNav = true;

// You can add logic for useWeekNav here based on:
// useWeekNav(initial) → {weekStart, next, prev, setWeek}
