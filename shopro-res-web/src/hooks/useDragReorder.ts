/*
 * useDragReorder<T>(items, onReorder) — drag state machine
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_useDragReorder = true;

// You can add logic for useDragReorder here based on:
// useDragReorder<T>(items, onReorder) — drag state machine
