/*
 * usePolling(fn, intervalMs, enabled) — live dashboard
 */

// Placeholder exports to ensure imports don't fail immediately
export const placeholder_usePolling = true;

// You can add logic for usePolling here based on:
// usePolling(fn, intervalMs, enabled) — live dashboard
